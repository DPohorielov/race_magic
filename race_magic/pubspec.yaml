name: race_magic
description: Chrono race app.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 0.0.1+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^1.4.0
  cloud_firestore: ^2.4.0

  json_annotation: ^4.0.1


  cupertino_icons: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.6
  json_serializable: ^4.1.4
  lint: ^1.5.3


flutter:

  uses-material-design: true

